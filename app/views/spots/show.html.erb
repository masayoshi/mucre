<div class="row">
  <div class="span12">
    <h4><i class="icon-map-marker"></i> 会場情報</h4>
    <dl>
      <dt>会場</dt>
      <dd><%= @spot.name %></dd>
      <dt>住所</dt>
      <dd><%= @spot.address %></dd>
    </dl>
    <%= gmaps(@gmaps_options) %>
  </div>
</div>
<%= link_to 'Edit', edit_spot_path(@spot) %> |
<%= link_to 'Back', spots_path %>
